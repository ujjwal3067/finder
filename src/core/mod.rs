#![allow(dead_code, unused_imports)]
use crate::parser; 

/// This function gets all the directory names relative to current path
pub fn get_directory_names() { 

} 

/// This function gets all the file names relative to current path 
pub fn get_file_names() { 

} 
